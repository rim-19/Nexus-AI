# --- web framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17        # file uploads

# --- config / validation ---
pydantic==2.10.3
pydantic-settings==2.6.1

# --- database (async SQLAlchemy + Postgres) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0                 # async driver for runtime
psycopg2-binary==2.9.10         # sync driver for scripts/migrations
pgvector==0.3.6                 # SQLAlchemy Vector type
alembic==1.18.5                 # migrations

# --- reliability + security ---
slowapi==0.1.9                  # rate limiting
aiofiles==24.1.0                # async file storage

# --- auth ---
python-jose[cryptography]==3.3.0
argon2-cffi==23.1.0

# --- http (AI providers) ---
httpx==0.28.1

# --- ingestion ---
pypdf==5.1.0                    # PDF text + page numbers
python-docx==1.1.2              # DOCX
tree-sitter==0.23.2             # structure-aware code chunking
tree-sitter-language-pack==0.4.0  # prebuilt grammars (py, js, ts, go, java, ...)
tiktoken==0.8.0                 # token counting for chunk sizing

# --- dev ---
pytest==8.3.4
pytest-asyncio==0.24.0
